services:
  - type: web
    name: hiring-agent-backend
    env: python
    repo: https://github.com/rqlaundry0219-ui/hiring_agent
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend.app.main:app --host 0.0.0.0 --port 10000"
    plan: free
